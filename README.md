# gitrs

Learning Rust and Git internals at the same time by building a simple
git clone in Rust.

## NOTE

Currently, the index is not compatible with the actual git index.
I am using a text-based index file, whereas Git uses binary data.

Eventually, I will start to implement Git's index format but for now
I am trying to get the basics working.
